#pragma strict
/*
import Leap;

private var controller : Controller;

var offset : float;

function Start () {
	controller = new Controller();
}

function Update () {
	var frame : Frame = controller.Frame();
	if (frame.Pointables.Count > 0) {
		transform.position.x = frame.Pointables[0].TipPosition.x;
		transform.position.y = frame.Pointables[0].TipPosition.y - 100;
		transform.position.z = -frame.Pointables[0].TipPosition.z + offset;
	}
	
}
*/